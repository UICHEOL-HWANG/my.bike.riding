# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

**수집기가 가동 중이다** (2026-09-06~). Supabase `pg_cron`이 10분마다 서울시 API를 호출해 관심 대여소 14곳의 재고를 적재한다. 스케줄러는 [docs/scheduler.sql](docs/scheduler.sql), 파이썬 경로는 수동 실행·백필용으로 유지한다.

```bash
uv run pytest                              # 테스트 (네트워크 불필요)
uv run python -m collector                 # 수집 1회 수동 실행
uv run python scripts/fetch_my_rides.py    # 본인 이용내역 적재
gh run list --workflow=collect --limit 5   # 스케줄 실행 상태
```

설정은 `.env`(`.env.example` 참고)와 `stations.yml`. 테이블 DDL은 [docs/schema.sql](docs/schema.sql).

## 프로젝트

서울시 공공자전거(따릉이) 대여소 재고를 직접 수집·적재하고, "특정 대여소에 특정 시각 자전거가 있을까"를 예측하는 개인 프로젝트. 상세 배경·스키마 초안·작업 순서는 인계서에 있다.

## 지금의 우선순위

수집기 최소 버전은 완료됐다. 그 뒤로는 급하지 않다.

다만 **수집이 멈추는 것은 여전히 최악이다.** 실시간 API(OA-15493)는 호출 시점만 주고 과거 조회 파라미터가 없어서, 안 돈 시간은 그대로 손실이다. 스케줄이 죽었는지 먼저 확인하는 습관을 유지할 것.

**단, 최근 7일은 예외다.** `대여소 운영현황(시간대별)`(OA-20447, 서비스명 `bikeListHist`)이 시간 단위 대여가능 건수를 최근 7일치 제공한다. 수집이 끊겨도 7일 안에 알아채면 백필할 수 있다 — `scripts/backfill_history.py`.

`stationDt`는 KST다(추측이 아니라 실측 대조로 확인했다). 시간 단위라 10분 격자는 복원하지 못하고 정시 지점만 메운다. 백필분은 `station_snapshot.source='hist'`로 구분되므로 분석 시 섞이지 않는다.

## 아키텍처 제약 (기존 개인 파이프라인 재사용)

- DB는 Supabase, 대시보드는 GitHub Pages 정적, 원본은 JSON 스냅샷 그대로 아카이브
- **별도 VM·데이터레이크는 도입하지 않는다.** 스케줄은 Supabase `pg_cron` + `pg_net`이다.
- **GitHub Actions cron은 쓰지 않는다 — 실측으로 배제했다.** `*/10` 예약이 6시간 30분 동안 1회만 발화했다(약 3%). 격자 내림은 "지연"은 흡수해도 "누락"은 못 막는다. pg_cron은 같은 조건에서 오차 100ms 이내로 매번 발화한다. 워크플로는 `workflow_dispatch` 전용으로 남겨 비상 경로로만 쓴다
- **크롤링도 도입하지 않는다.** 본인 이용내역만 예외인데, 이것도 로그인 자동화가 아니라 **사람이 브라우저에서 만든 세션을 재사용**한다(`BIKESEOUL_SESSION`). 자동 로그인은 실제로 시도했다가 봇 판별에 차단당했다. 한 달에 한 번 돌릴 스크립트를 위해 그걸 뚫을 이유가 없다
- 실패가 조용히 방치되는 게 최악이다. 스케줄 등록과 실패 알림은 세트로 간다

## 데이터 모델링 시 반드시 지킬 것

- **대여이력(OA-15182)은 트랜잭션이지 재고가 아니다.** 대여/반납 이벤트를 누적해도 재배치 트럭 물량과 초기값을 모르므로 절대 재고는 복원 불가. 재고는 스냅샷으로만 얻는다 (예외는 위의 OA-20447 7일 창)
- **공공 오픈데이터로는 본인 이용내역을 복원할 수 없다.** 이용정보(OA-15246)·승객수(OA-21229)는 전부 집계본이라 개인 식별자가 없다. 본인 계정에서만 얻는다 — `collector/my_ride.py`
- API는 **1회 호출당 최대 1,000건**이라 페이징 필수
- 대여소가 수천 개라 전량을 5분 간격으로 적재하면 한 달에 수천만 행 — Supabase 무료 구간을 넘긴다. **관심 대여소 + 인접 몇 개만 필터링해 적재하고, 대상 목록은 설정 파일로 분리**할 것(범위를 넓힐 때 코드를 고치지 않도록)
- `station_snapshot`은 `(station_id, captured_at)` unique. 스키마는 수정해도 되지만 의도는 유지

## 모델링 단계 (아직 착수 전)

- 이진 분류("1대 이상 있는가")부터. 대수 회귀는 그다음
- **리크 금지**: 예측 시점에 알 수 없는 값 금지. 내일 아침을 예측하며 내일 아침 *실측* 강수량을 쓰면 리크 — 예보값을 쓰거나 예보 시점 기준으로 자를 것
- 시계열이므로 랜덤 분할 금지, 시간 순 분할
- 사용자 본인의 대여내역 중 **평소와 다른 대여소에서 빌린 날**이 곧 "주 대여소 재고 실패" 정답지다

## 정해진 것

- 수집 간격 **10분** / 관심 대여소 **14곳**(`stations.yml`, 본인 이용내역 168건의 빈도로 도출)
- 대여내역은 **세션 재사용 방식**으로 자동화(`scripts/fetch_my_rides.py`). 조회 상한이 있어 6개월 창으로 나눠 받는다

## 다음 과제

- 대시보드 "마지막 수집: N분 전" — GitHub은 public 저장소에서 60일 무활동 시 스케줄 워크플로를 자동 비활성화한다. 이 수집기는 커밋할 일이 없어 정확히 여기 걸린다
- 날씨 조인, 베이스라인 모델
