# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

코드가 아직 없다. 저장소에는 [ttareungi-handoff.md](ttareungi-handoff.md) 인계서 한 건만 있고 git 저장소도 아니다. 빌드·테스트·린트 명령은 아직 존재하지 않으므로, 첫 코드를 작성할 때 실행 방법을 이 파일에 함께 추가할 것.

## 프로젝트

서울시 공공자전거(따릉이) 대여소 재고를 직접 수집·적재하고, "특정 대여소에 특정 시각 자전거가 있을까"를 예측하는 개인 프로젝트. 상세 배경·스키마 초안·작업 순서는 인계서에 있다.

## 지금의 우선순위

**수집기가 1순위다.** 서울시 실시간 대여정보 API(OA-15493)는 호출 시점 스냅샷만 주고 과거 조회 파라미터가 없다. 학습 데이터는 수집기를 돌린 이후로만 쌓이므로, 착수가 늦어진 만큼 그대로 손실이다. 다른 작업(파일데이터 다운로드, 날씨 조인, 분석, 모델링)을 먼저 하자는 방향으로 유도하지 말 것.

최소 버전은 `API 호출 → 관심 대여소 필터 → Supabase insert` 까지다. 이게 돌아가면 그 뒤로는 급하지 않다.

## 아키텍처 제약 (기존 개인 파이프라인 재사용)

- DB는 Supabase, 대시보드는 GitHub Pages 정적, 원본은 JSON 스냅샷 그대로 아카이브
- **별도 VM·데이터레이크·크롤링은 도입하지 않는다.** 스케줄러도 VM 없이 가야 하므로 Supabase `pg_cron` + `pg_net`을 우선 검토. GitHub Actions cron은 예약 시각 지연이 커서 5~10분 간격에는 부적합할 수 있으니, 쓴다면 실제 지연을 먼저 측정할 것
- 실패가 조용히 방치되는 게 최악이다. 스케줄 등록과 실패 알림은 세트로 간다

## 데이터 모델링 시 반드시 지킬 것

- **대여이력(OA-15182)은 트랜잭션이지 재고가 아니다.** 대여/반납 이벤트를 누적해도 재배치 트럭 물량과 초기값을 모르므로 절대 재고는 복원 불가. 재고는 스냅샷으로만 얻는다
- API는 **1회 호출당 최대 1,000건**이라 페이징 필수
- 대여소가 수천 개라 전량을 5분 간격으로 적재하면 한 달에 수천만 행 — Supabase 무료 구간을 넘긴다. **관심 대여소 + 인접 몇 개만 필터링해 적재하고, 대상 목록은 설정 파일로 분리**할 것(범위를 넓힐 때 코드를 고치지 않도록)
- `station_snapshot`은 `(station_id, captured_at)` unique. 스키마는 수정해도 되지만 의도는 유지

## 모델링 단계 (아직 착수 전)

- 이진 분류("1대 이상 있는가")부터. 대수 회귀는 그다음
- **리크 금지**: 예측 시점에 알 수 없는 값 금지. 내일 아침을 예측하며 내일 아침 *실측* 강수량을 쓰면 리크 — 예보값을 쓰거나 예보 시점 기준으로 자를 것
- 시계열이므로 랜덤 분할 금지, 시간 순 분할
- 사용자 본인의 대여내역 중 **평소와 다른 대여소에서 빌린 날**이 곧 "주 대여소 재고 실패" 정답지다

## 미결정 사항 (수집기 돌린 뒤 정해도 됨)

수집 간격(5분/10분), 관심 대여소 반경, 대여내역 기록 자동화 여부.
