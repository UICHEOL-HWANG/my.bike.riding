# 따릉이 데이터 파이프라인 — 작업 인계서

## 0. 한 줄 요약

서울시 공공자전거(따릉이) 대여소 재고를 직접 수집해서 쌓고, "내가 타려는 시간에 그 대여소에 자전거가 있을까"를 예측하는 개인 프로젝트. 지금 필요한 건 모델이 아니라 **수집기**다.

---

## 1. 배경과 동기

- 매일 아침 학원 갈 때, 저녁에 돌아올 때 따릉이를 탄다. 하루 2회.
- 이용 대여소는 거의 고정인데, 자전거가 없어서 못 타는 날이 잦다. 그럴 때는 다른 대여소로 걸어간다.
- 이 불편이 곧 예측 문제다. 개인적으로도 쓸모 있고 포트폴리오 스토리도 자연스럽게 나온다.

## 2. 왜 수집기가 먼저인가

서울시 실시간 대여정보 Open API는 **호출한 그 순간의 스냅샷만** 준다. 과거 시점을 조회하는 파라미터가 없다.

즉 학습 데이터는 시간이 지나야 생긴다. 다른 작업은 나중에 몰아서 해도 되지만 수집 착수가 늦어진 만큼은 그대로 손해다. **오늘 최소 버전이라도 돌려놓는 게 이 프로젝트의 1순위.**

---

## 3. 데이터 소스 3종

| 구분 | 역할 | 상태 |
|---|---|---|
| 실시간 API 스냅샷 (직접 수집) | 모델 학습용 피처 | **미착수 — 최우선** |
| 공공 파일데이터 (과거) | 요일×시간대 패턴 파악, 피처 설계 근거 | 다운로드만 하면 됨 |
| 내 대여내역 | 예측 검증용 정답지 | 수기 기록 중 |

### 3-1. 실시간 API (수집 대상)

- 서울 열린데이터광장: `서울시 공공자전거 실시간 대여정보` (OA-15493)
- 제공 항목: 대여소별 대여가능 건수, 거치율, 총 거치대 수, 위경도
- **한 번 호출에 최대 1,000건 제한** → 페이징 필요
- 인증키는 열린데이터광장에서 발급

### 3-2. 공공 파일데이터

먼저 열어서 **실제 수록 구간과 갱신 여부부터 확인할 것.** 아래 첫 번째가 최근까지 이어져 있으면 수집 시작 이전 구간까지 학습 데이터가 생겨서 일정이 몇 달 당겨진다.

- `서울특별시_대여소별 공공자전거 대여가능 수량(1시간 단위)` — 과거 재고 시계열. 파일명이 20230331로 끝나 있어 갱신 여부 확인 필요
- `서울시 공공자전거 따릉이 대여이력 정보` (OA-15182) — 대여소별·자전거별 원천 트랜잭션. 용량 큼
- `서울시 공공자전거 이용정보(시간대별)` (OA-15245) — 위 이력의 시간대별 집계본. 더 가볍다
- `일별 대여소별 거치수량 (2019~2021)` (OA-22223) — 구간이 오래돼서 참고용

> 주의: 대여이력은 트랜잭션이라 **재고가 아니다.** 대여/반납 이벤트를 누적하면 순증감은 나오지만, 재배치 트럭 물량과 초기값을 모르니 절대 재고는 복원 불가. 재고는 스냅샷으로만 얻는다.

### 3-3. 내 대여내역

- 하루 2건, 출발/도착 대여소와 시각
- 특히 **평소와 다른 대여소에서 빌린 날**이 중요하다. 주 대여소 재고가 실패했다는 기록이라 별도 라벨링 없이 정답지가 된다

---

## 4. 인프라 방침

기존 개인 데이터 파이프라인(틴더 프로젝트)에서 이미 정한 구성을 그대로 재사용한다. 테이블만 추가하는 수준.

- **DB**: Supabase
- **대시보드**: GitHub Pages (정적)
- **원본 보관**: JSON 스냅샷 그대로 아카이브
- **도입하지 않음**: 별도 VM, 데이터레이크, 크롤링

스케줄러는 VM 없이 가야 하므로 Supabase의 pg_cron + pg_net 조합을 우선 검토. GitHub Actions cron은 예약 시각이 상당히 밀릴 수 있어서 5~10분 간격 수집에는 부적합할 가능성이 있다 — 쓰기로 한다면 실제 실행 지연을 먼저 측정해볼 것.

---

## 5. 스키마 초안

수정해도 되지만 의도는 유지할 것.

```sql
-- 대여소 재고 스냅샷
create table station_snapshot (
  id            bigserial primary key,
  captured_at   timestamptz not null,
  station_id    text        not null,
  parking_cnt   int,          -- 대여 가능한 자전거 수
  rack_total    int,          -- 총 거치대 수
  shared        int,          -- 거치율
  created_at    timestamptz default now(),
  unique (station_id, captured_at)
);
create index on station_snapshot (station_id, captured_at desc);

-- 대여소 마스터 (이름, 좌표는 자주 안 변하니 분리)
create table station (
  station_id    text primary key,
  name          text,
  lat           double precision,
  lon           double precision,
  updated_at    timestamptz
);

-- 내 대여 기록
create table my_ride (
  id              bigserial primary key,
  rented_at       timestamptz not null,
  returned_at     timestamptz,
  from_station_id text,
  to_station_id   text,
  is_fallback     boolean,     -- 평소 대여소가 아닌 곳에서 빌렸는지
  note            text
);
```

### 저장량 주의

따릉이 대여소는 수천 개다. 전체를 5분 간격으로 저장하면 하루 수십만 행, 한 달이면 수천만 행이 된다. Supabase 무료 구간에서 감당하기 어렵다.

→ **관심 대여소 + 반경 내 인접 대여소 몇 개만 필터링해서 적재.** 대상 목록은 설정 파일로 빼둘 것. 나중에 넓히고 싶어질 때 코드를 안 고치게.

---

## 6. 작업 순서

1. **수집기 최소 버전** — API 호출 → 관심 대여소 필터 → Supabase insert. 여기까지 되면 데이터가 쌓이기 시작하므로 이 시점부터는 급하지 않다
2. 스케줄 등록 + 실패 알림 (조용히 멈춰 있는 게 최악)
3. 원본 JSON 아카이브 (스키마를 나중에 바꿔도 재처리할 수 있게)
4. 공공 파일데이터 다운로드 + 수록 구간 확인
5. 기상청 ASOS 과거관측 조인 (성능 차이가 크다)
6. 내 대여내역 정리 및 적재
7. 탐색적 분석 — 요일×시간대별 재고 프로파일
8. 모델링

---

## 7. 모델링 단계에서 지킬 것 (지금은 참고만)

- **문제 정의**: "특정 대여소, 특정 시각에 자전거가 1대 이상 있을까" 이진 분류부터. 대수 회귀는 그다음
- **피처**: 시간대, 요일, 공휴일, lag(1시간 전 / 24시간 전 / 지난주 같은 시각), 인접 대여소 재고, 날씨
- **리크 금지**: 예측 시점에 알 수 없는 값을 넣지 말 것. 내일 아침을 예측하면서 내일 아침 *실측* 강수량을 쓰면 리크다. 예보값을 쓰거나, 최소한 예보 시점 기준으로 자를 것
- **검증**: 시계열이므로 랜덤 분할 금지. 시간 순 분할로

### 곁다리로 붙는 것들

- 대여소 클러스터링 — 시간대별 재고 패턴 벡터로 묶으면 출근형/퇴근형/관광형이 갈린다. 라벨 불필요
- 재배치 탐지 — 재고가 자연 증감 범위를 벗어나 튀는 구간이 트럭이 다녀간 시점. 이상탐지 소재

---

## 8. 지금 당장 결정 안 된 것

- 수집 간격 (5분 / 10분) — 저장량과 해상도 트레이드오프
- 관심 대여소 반경 (도보 몇 분까지를 대안으로 볼 것인지)
- 대여내역 기록 자동화 여부 (현재 수기)

이건 수집기 돌려놓고 정해도 늦지 않다.
